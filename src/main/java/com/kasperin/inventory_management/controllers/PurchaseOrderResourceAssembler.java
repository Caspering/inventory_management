/*
package com.kasperin.inventory_management.controllers;

import static org.springframework.hateoas.mvc.ControllerLinkBuilder.*;

import org.springframework.hateoas.Resource;
import org.springframework.hateoas.ResourceAssembler;
import com.kasperin.inventory_management.controllers.v1.PurchaseOrderController;
import com.kasperin.inventory_management.domain.commerce.PurchaseOrder;
import org.springframework.stereotype.Component;

@Component
public class PurchaseOrderResourceAssembler
        implements ResourceAssembler<PurchaseOrder, Resource<PurchaseOrder>>{

        */
/*@Override
        public Resource<PurchaseOrder> toResource (PurchaseOrder purchaseOrder){

            return new Resource<>(purchaseOrder,
                linkTo(methodOn(PurchaseOrderController.class).one(purchaseOrder.getId())).withSelRel(),
                linkTo(methodOn(PurchaseOrderController.class).all()).withRel("purchase_orders"));
        }*//*

}*/
